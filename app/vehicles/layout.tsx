import { DashboardLayout } from "@/layouts/dashboard-layout";

export default function VehiclesEntityLayout({ children }: { children: React.ReactNode }) {
  return <DashboardLayout>{children}</DashboardLayout>;
}
